# Software-development example

This example describes the desired software-development harness rather than a
particular provider, model, agent host, or solution to the fixture task.

An implementation binding may select any compatible model, toolchain, runtime,
sandbox, and evaluator that satisfies the declared capabilities and does not
widen authority. The example's external evaluator remains outside the harness.
